machine:
  ruby:
    version: 2.3.1
  node:
    version: 5.3.0
deployment:
  production:
    owner: rossta
    branch: development
    commands:
      - bin/rake deploy
dependencies:
  cache_directories:
    - node_modules
  pre:
    - gem uninstall bundler
    - gem install bundler -v '1.12.5'
test:
  override:
