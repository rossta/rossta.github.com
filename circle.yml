machine:
  environment:
    USER_NAME: rossta
    USER_EMAIL: rosskaff@gmail.com
  ruby:
    version: 2.3.1
  node:
    version: 5.3.0
dependencies:
  pre:
    - gem install bundler -v 1.12.5
compile:
  override:
    - bundle exec rake deploy
