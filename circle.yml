machine:
  ruby:
    version: 2.3.1
  node:
    version: 5.3.0
deployment:
  production:
    owner: rossta
    branch: develop
    commands:
      - bin/rake deploy
dependencies:
  cache_directories:
    - node_modules
test:
  override:
    - echo Y
