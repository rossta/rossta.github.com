machine:
  ruby:
    version: 2.3.1
  node:
    version: 5.3.0
dependencies:
  cache_directories:
    - node_modules
database:
  override:
    - echo Y
test:
  override:
    - echo Y
checkout:
  post:
    - bin/rake deploy
