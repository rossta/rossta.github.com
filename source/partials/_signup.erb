<!-- Begin MailChimp Signup Form -->
<% prompt = "Stay in the LOOP!" %>
<div class="signup-form">
  <h3><%= title || prompt %></h3>
  <p>
    <% if current_article.present? %>
      <%= tweet_link_to "Do me a favor and share this post on Twitter", {
        text: current_article.title,
        url: to_url(path: current_article.url)
      } %>
      and
    <% else %>
      Please
    <% end %>
    allow me to send you an occasional email about
    <%= current_page_tags.join(', ') %> and the web. Thank you!
  </p>
  <div id="mc_embed_signup">
    <form action="<%= signup_form_url %>" method="post" id="mc-embedded-subscribe-form" name="mc-embedded-subscribe-form" class="validate" target="_blank" novalidate>
      <div id="mc_embed_signup_scroll">

        <div class="row">
          <div class="large-10 columns">
            <div class="row collapse">
              <div class="large-8 small-7 columns">
                <input type="email" placeholder="Your email..." value="" name="EMAIL" class="required email" id="mce-EMAIL">
              </div>
              <div class="large-4 small-5 columns">
                <input type="submit" value="Sign me up!" name="subscribe" id="mc-embedded-subscribe" class="button postfix">
              </div>
            </div>
          </div>
        </div>

        <div id="mce-responses" class="clear">
          <div class="response" id="mce-error-response" style="display:none"></div>
          <div class="response" id="mce-success-response" style="display:none"></div>
        </div>
        <!-- real people should not fill this in and expect good things - do not remove this or risk form bot signups-->
        <div style="position: absolute; left: -5000px;"><input type="text" name="b_8ce159842b5c98cecb4ebdf16_96030b0bda" tabindex="-1" value=""></div>
      </div>

      <input type="hidden" name="SIGNUP" value="<%= current_url %>" />
      <input type="hidden" name="SIGNUP_APP" value="Mailchimp" />
      <input type="hidden" name="SIGNUP_LOCATION" id="signup_location" value="<%= current_page.path %>" />
      <% current_page_tags.each_with_index do |tag, i| %>
        <input type="hidden" name="group[17961][<%= i+1 %>]" id="signup_technology" value="<%= tag %>" />
      <% end %>
    </form>
  </div>
  <!--End mc_embed_signup-->
</div>
