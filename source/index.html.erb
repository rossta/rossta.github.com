---
pageable: true
per_page: 4
---
<% content_for(:title) { } %>
<% content_for(:section) { 'I make things' } %>

<section id="hero">
  <header>
    <h1>Hello! I'm Ross. I build web apps.</h1>
  </header>
  <section>
    <p>Building apps for the web is HARD. Here's where I share what I've learned through my
      <%= link_to "writing", "/blog" %>,
      <%= link_to "talks", '/talks' %> and
      <%= link_to "projects", "/projects" %>. You can also connect with me on
      <%= link_to "Twitter", "https://twitter.com/rossta" %> and
      <%= link_to "Medium", "https://medium.com/@rossta" %> where I talk about other stuff too.</p>
  </section>
  <%= partial "partials/signup" %>
</section>

<h2 class="margin-bottom">New? Start here</h2>
<section class="index-posts margin-top">
  <% blog_series.each do |series| %>
    <article class="index-post">
      <header>
        <h4><%= link_to series.title, series_path(series.id, "blog") %> <small>&middot; SERIES</small></h4>
        <p class="alt-title subtle"><%= series.summary %></p>
      </header>
    </article>
  <% end %>
  <% page_articles.each do |article| %>
    <article class="index-post">
      <header>
        <h4>
          <%= link_to article.title, article %>
        </h4>
        <% if article.data.summary %>
          <p class="alt-title subtle"><%= article.data.summary %></p>
        <% end %>
      </header>
    </article>
  <% end %>
</section>
